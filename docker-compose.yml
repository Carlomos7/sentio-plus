services:
  api:
    build: ./app
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data:ro
    env_file:
      - app/.env
  chromadb:
    image: chromadb/chroma:latest
    ports:
      - "8001:8000"
    volumes:
      - ./app/chroma_data:/data